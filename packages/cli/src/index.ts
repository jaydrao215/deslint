#!/usr/bin/env node

/**
 * @vizlint/cli — Design quality analysis CLI.
 * Implements: vizlint scan, vizlint fix, vizlint init
 * Full implementation in Sprint 5 (VIZ-012).
 */

export const VERSION = '0.1.0';
